<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campus.runner.mapper.UserMapper">

    <!-- 根据 ID 查询用户 -->
    <select id="getUserById" parameterType="long"
            resultType="com.campus.runner.entity.User">
        SELECT id, username, password, phone, role, create_time as createTime
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 根据用户名查询 -->
    <select id="getUserByUsername" parameterType="string"
            resultType="com.campus.runner.entity.User">
        SELECT id, username, password, phone, role, create_time as createTime
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 获取所有用户 -->
    <select id="selectAllUsers" resultType="com.campus.runner.entity.User">
        SELECT id, username, password, phone, role, create_time as createTime
        FROM user
        ORDER BY create_time DESC
    </select>

    <update id="updateById">
        UPDATE user
        SET role = #{role}
        WHERE id = #{id}
    </update>

    <update id="updateUserStatus">
        UPDATE user
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.campus.runner.entity.User">
        INSERT INTO user(username, password, phone, role, create_time)
        VALUES (#{username}, #{password}, #{phone}, #{role}, NOW())
    </insert>

</mapper>
